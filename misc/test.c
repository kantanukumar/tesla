#include <stdio.h>

int main(int argc, char **argv)
{

  



  /* 
            int numbers[ 10 ];
            int i, index = 2;
            
            for( i = 0; i < 10; i++ ) 
                numbers[ i ] = i * 10;
            
            numbers[ 8 ] = 25;
            numbers[ 5 ] = numbers[ 9 ] / 3;
            numbers[ 4 ] += numbers[ 2 ] / numbers[ 1 ];
            numbers[ index ] = 5;
            ++numbers[ index ];
            numbers[ numbers[ index++ ] ] = 100;
            numbers[ index ] = numbers[ numbers[ index + 1 ] / 7 ]--;
            
            for( index = 0; index < 10; index++ )
                printf( "numbers[ %d ] = %d\n", index, numbers[ index ] );
                
               // result numbers = {0, 10, 6, 100, 42, 30, 99, 70, 25, 90}
  */

  /* Not allowed before c99
  int len = 5;
  int arr[len] = {0};
  */
}
