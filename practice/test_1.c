#include <stdio.h>
#include <string.h>

int main()
{
  int len = 0;
  int len2 = 0;
  char ch = 0;

  char arr[] = "Kantanu";

  len = sizeof("KK");
  len2 = strlen("KK");
  ch = sizeof(arr);
  len = strlen(arr);
  return 0;
}
